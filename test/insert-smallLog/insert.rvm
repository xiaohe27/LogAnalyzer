package rvm;
  Insert(String user,String db) {
 // String user;
 // String db;

  event insert (String user,String db,String p,String data, boolean[] result) {
		if(db.equals("db2") && !user.equals("script1"))
		{
			result[0] = true;
		}
	}
}
