package rvm;

  Insert(String user,String db,String p,String data) {
  String user;
  String db;
  String p;
  String data;
  long time;



  event insert (String user,String db,String p,String data, long time) {
        System.out.println(user+" insert "+ data+" to "+ db+" at time "+time);

		if(db.equals("db2"))
		{
		if(user.equals("script1")) {}
		else{
		    System.out.println("@"+this.time+" User "+user+" insert to db2 the data: "+data);
		    return;
		}
		}

		else{}
	}

}
