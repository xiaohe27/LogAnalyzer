1. measure base execution time (read all insert) on cur settings 

2. test the time for reading all events (use a fake one for Monpoly)



 try the little insert log.

 try the huge log on the insert property, record the time.
